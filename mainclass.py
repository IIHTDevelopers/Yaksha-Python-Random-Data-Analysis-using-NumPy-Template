import numpy as np

class RandomDataAnalysis:
    def __init__(self):
        pass
        """Initialize by generating 100 random integers."""
        # self.data = np.random.randint(1, 1001, size=100)

    def calculate_mean(self):
        pass
        """Calculate and return the mean of the data."""
        # if self.data.size == 0:
        #     raise ValueError("Cannot compute mean for empty data")
        # return np.mean(self.data)

    def calculate_median(self):
        pass
        """Calculate and return the median of the data."""
        # if self.data.size == 0:
        #     raise ValueError("Cannot compute median for empty data")
        # return np.median(self.data)